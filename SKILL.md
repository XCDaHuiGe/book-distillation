---
name: book-distillation-v4
description: "长文本深度蒸馏系统 V4.0。通过逻辑种子提取、Python认知图谱构建与3-Agent智能体检索协作，将数十万字巨著转化为双轨产出：Markdown可生长知识库（[[双链]]驱动）+ 杂志风HTML PPT。GraphRAG+Agentic RAG驱动，Karpathy Wiki底层架构，10种杂志布局模板。"
---

# 书籍深度蒸馏 · 认知图谱与杂志风内参 (V4.0)

## 核心设计哲学

> **高维压缩，逻辑溯源，智能体检索。**
> 针对长文本，先提取"逻辑种子"以建立全局视野，再通过Python构建认知图谱实现结构化索引。
> Agent 不再被动接收信息，而是通过Bash调用Python检索脚本进行多跳推理。
> 视觉服务于逻辑：每一页 HTML 必须在 5 秒内传递一个核心决策点。

> **杂志风设计哲学：**
> 1. **克制优于炫技** — WebGL 背景只在 hero 页透出，平凡页几乎看不见
> 2. **结构优于装饰** — 不用阴影、不用浮动卡片、不用 padding box，一切信息靠字号 + 字体对比 + 网格留白
> 3. **字体分工法则** — 衬线（标题/重点/数字）→ 视觉重音；非衬线（正文）→ 信息密度；等宽（kicker/meta/foot）→ 装饰节奏
> 4. **图片第一公民** — 只裁底部，保证顶部和左右完整；网格用固定 height，不用 aspect-ratio
> 5. **节奏靠 hero 页** — hero 和 non-hero 交替，不累眼睛

## V4.0 核心升级

- **双轨产出**：路径A → Markdown 可生长知识库（面向 AI 问答 + 增量更新）；路径B → 杂志风 HTML PPT（面向人类阅读）
- **Wiki 知识库骨架**：Phase 0 自动生成 INDEX.md + concepts/ / actions/ / boundaries/ / entities/ 目录
- **[[双链]] 体系**：3-Agent 产出使用 Markdown 内部链接，概念间可追溯、可交叉引用
- **10 种杂志布局模板**：hero-cover / act-divider / big-numbers / text-image-split / image-grid / pipeline / question-page / big-quote / before-after / mixed-layout
- **模块8：边界约束**：新增红线清单 + 致命误区 + 适用边界，避免知识误用
- **5 套主题色**：墨水经典 / 靛蓝瓷 / 森林墨 / 牛皮纸 / 沙丘，T 键切换

## 触发指令

```
/book-distillation <file_path> [style] [output_mode]
```

- `<file_path>`：必选。支持 .epub / .txt / .pdf / .md
- `[style]`：可选。"麦肯锡" / "极简" / "学术" / "科技感" / "暖色" / "安全对话" / "杂志风" / "magazine"。未提供时智能匹配书籍情感基调
- `[output_mode]`：可选。"both"（默认，双轨产出）/ "kb-only"（仅知识库）/ "html-only"（仅 HTML PPT）

---

## 超长文本处理策略

### 文本分级与处理路径

```
文本量级判定
├─ ≤10万字 → 标准路径（全文直传3个Agent）
├─ 10-30万字 → 分层压缩路径（逻辑种子 + 章节摘要 + 关键段落直传）
├─ 30-60万字 → 三级蒸馏路径（逻辑种子 → 章节蒸馏 → 段落精炼 → Agent）
└─ >60万字 → Python认知图谱+智能体检索路径（逻辑种子 + FAISS向量索引 + NetworkX知识图谱 + Agent多跳推理检索）
```

### 分层压缩机制（10-60万字）

**第一层：逻辑种子（2000-3000 Token）**
- 全书骨架：章节标题 + 每章一句话核心论点
- 作者专属术语表（Unique Glossary）
- 论点递进线索图（Logic Thread）
- 核心隐喻与价值观（Meta-Value）

**第二层：章节蒸馏摘要（每章 500-800 Token）**
- 每章提取：核心论点 + 关键论据 + 作者原话（1-2句）
- 保留章节间的逻辑衔接标记
- 标记"高密度章节"，后续可按需展开

**第三层：关键段落直传（按需展开）**
- 仅对"高密度章节"展开原始段落
- 展开范围控制在总文本的 20% 以内

### Python认知图谱+智能体检索路径（>60万字）【V3核心升级，V4保留】

**预处理（Phase 0中由主控Agent执行Python脚本）：**

1. **文本分段**：将全书按段落切分（每段≤500字），生成 `segments.json`
2. **向量语义层**：调用Python脚本生成段落向量索引（FAISS），语义标签
3. **结构图谱层**：调用Python脚本提取核心实体与关系，构建NetworkX知识图谱
4. **双索引对齐**：建立图谱节点/边与向量段落的双向映射指针

**Python检索工具箱（Agent通过Bash调用）：**

Agent 不再接收全文，而是通过Bash调用以下Python脚本进行多轮推理检索：

| 命令 | 适用场景 | 示例 |
|:---|:---|:---|
| `python /tmp/book_rag/search.py "查询内容" 5` | 语义相似度检索 | 查找"对比说明法的操作步骤" |
| `python /tmp/book_rag/graph.py --entity "安全感" --depth 2` | 图谱结构检索 | 查找"安全感"相关的实体和关系 |
| `python /tmp/book_rag/multihop.py "沉默" "共同目的" 3` | 多跳推理 | 从"沉默"到"共同目的"的推理链 |

**Agentic Loop 检索模式：**
```
Agent评估当前信息缺口
  → 通过Bash调用Python检索脚本
    → 评估返回结果是否足够
      → 若不足：换工具或修改查询再次检索
      → 若充足：生成最终内容
```

---

## Python工具链规范

### 依赖库

```bash
pip install sentence-transformers faiss-cpu networkx jieba
```

> 如果环境中未安装，Phase 0会自动安装。若安装失败，降级为"主控Agent深度阅读路径"。

### 文件结构

所有Python脚本和索引文件存放在 `/tmp/book_rag/` 目录：

```
/tmp/book_rag/
├── segments.json          # 分段后的文本段落（含章节、段落ID、原文）
├── entities.json          # 实体列表（名称、类型、出现段落）
├── relations.json         # 关系列表（头实体、尾实体、关系类型、证据段落）
├── graph.gpickle          # NetworkX知识图谱（序列化）
├── faiss.index            # FAISS向量索引
├── metadata.json          # 元数据（书名、作者、章节数、总段落数等）
├── search.py              # 语义检索脚本
├── graph.py               # 图谱检索脚本
├── multihop.py            # 多跳推理脚本
└── build_index.py         # 索引构建脚本（Phase 0调用）
```

### 检索脚本接口规范

**search.py — 语义检索：**
```bash
# 用法
python /tmp/book_rag/search.py "<查询文本>" [top_k] [--chapter "章节名"]

# 输出格式（JSON）
{
  "results": [
    {"segment_id": 42, "chapter": "第3章", "text": "...", "score": 0.87},
    ...
  ]
}
```

**graph.py — 图谱检索：**
```bash
# 用法
python /tmp/book_rag/graph.py --entity "安全感" [--relation "因果"] [--depth 2]

# 输出格式（JSON）
{
  "entity": "安全感",
  "related": [
    {"entity": "共同目的", "relation": "因果", "path": ["安全感", "相互尊重", "共同目的"], "evidence": ["段落ID: 42", "段落ID: 87"]},
    ...
  ]
}
```

**multihop.py — 多跳推理：**
```bash
# 用法
python /tmp/book_rag/multihop.py "起始实体" "目标实体" [max_hops]

# 输出格式（JSON）
{
  "start": "沉默",
  "end": "共同目的",
  "paths": [
    {"hops": 3, "path": ["沉默", "暴力", "安全感", "共同目的"], "evidence_segments": [12, 45, 89]},
    ...
  ]
}
```

---

## 工作流

### Phase 0: 逻辑种子与Python图谱构建 + Wiki骨架

**目标：** 解决长文本导致的"Agent 疲劳"、"上下文丢失"与"关系割裂"；为双轨产出建立知识库骨架。

1. **全局扫描**：识别书名、作者、核心隐喻及全书分析框架。
2. **文本分级**：判定字数量级，选择对应处理路径。
3. **逻辑种子生成**：提取 2000-3000 Token 的精华索引，包含：
   - **Unique Glossary**：作者定义的专属术语表
   - **Logic Thread**：论点随章节演进的递进线索图
   - **Meta-Value**：书籍底层价值观
   - **Density Map**：章节密度标注
4. **分层压缩**（>10万字时）：生成章节蒸馏摘要，标记高密度章节。
5. **Python认知图谱构建**（>60万字时）：
   - **Step 1**：部署工具到 `/tmp/book_rag/`：
     ```bash
     python ~/.claude/skills/book-distillation/tools/setup.py /tmp/book_rag
     ```
   - **Step 2**：将全书文本按段落切分，生成 `segments.json`：
     ```bash
     python /tmp/book_rag/segment_text.py /path/to/full_text.txt /tmp/book_rag/segments.json --max_len 500
     ```
   - **Step 3**：运行索引构建，生成FAISS向量索引 + NetworkX知识图谱：
     ```bash
     python /tmp/book_rag/build_index.py /tmp/book_rag/segments.json /tmp/book_rag
     ```
   - **Step 4**：验证索引完整性（至少1条跨章节实体关系链）
   - **Step 5**：验证检索工具可用：
     ```bash
     python /tmp/book_rag/search.py "测试查询" 3
     python /tmp/book_rag/graph.py --stats
     ```
6. **Wiki 知识库骨架创建**（V4.0 新增）：
   - **Step 6**：创建 `{书名}-kb/` 目录结构，具体布局如下：
     ```
     {书名}-kb/
     ├── wiki/
     │   ├── INDEX.md              # 路由文件：4个版块入口
     │   ├── concepts/             # 核心概念页面（模块2+3）
     │   │   └── example.md        # 每个概念一个独立文件
     │   ├── actions/              # 行动指南页面（模块4）
     │   │   └── example.md
     │   ├── boundaries/           # 边界约束页面（模块8）
     │   │   └── example.md
     │   └── entities/             # 实体索引页面
     │       └── example.md
     └── output/                   # 最终交付物
     ```
   - **Step 7**：生成 `wiki/INDEX.md`（面向 AI 的路由文件），包含：
     - 🎯**核心支柱** — 每个支柱名 + 一句话摘要 + `[概念名](wiki/concepts/概念名.md)`
     - 💡**颠覆认知** — 每个洞见名 + 一句话摘要 + `[洞见名](wiki/concepts/洞见名.md)`
     - 🪜**落地行动** — 每个行动名 + 一句话摘要 + `[行动名](wiki/actions/行动名.md)`
     - 🚫**边界约束** — 每个约束名 + 一句话摘要 + `[约束名](wiki/boundaries/约束名.md)`
   - **Step 8**：基于逻辑种子，预填充 INDEX.md 条目（无需写入具体概念文件，留待 Phase 1 填充）

   **INDEX.md 模板示例：**
   ```markdown
   # {书名} · 知识库路由

   ## 🎯 核心支柱
   - [支柱1名称](wiki/concepts/支柱1.md) — 一句话摘要
   - [支柱2名称](wiki/concepts/支柱2.md) — 一句话摘要

   ## 💡 颠覆认知
   - [洞见1名称](wiki/concepts/洞见1.md) — 一句话摘要

   ## 🪜 落地行动
   - [行动1名称](wiki/actions/行动1.md) — 一句话摘要

   ## 🚫 边界约束
   - [约束1名称](wiki/boundaries/约束1.md) — 一句话摘要
   ```

7. **创建任务列表**。

**质量门控：**
- 逻辑种子必须包含至少3个作者专属术语 + 1条完整的递进线索
- >60万字时，知识图谱必须包含至少1条跨章节的实体关系链
- 否则重新提取或重新构建图谱

**降级策略：** 若Python依赖安装失败或图谱构建失败，降级为"主控Agent深度阅读路径"（主控Agent用Read逐章精读，手动构建逻辑图谱摘要，分发给子Agent）。

---

### Phase 1: 3-Agent 逻辑蒸馏（V4.0 升级版）

*所有 Agent 同步接收 {逻辑种子} + {章节蒸馏摘要}（若有）。*
*对于 >60万字文本，Agent 通过Bash调用Python检索脚本按需获取原文细节，禁止凭空捏造。*

**通用新增（V4.0）：**
- 【设计约束】标记输出的每个模块适合哪种布局类型（hero-cover / act-divider / big-numbers / text-image-split / image-grid / pipeline / question-page / big-quote / before-after / mixed-layout）

---

#### Agent Alpha: 架构师 (模块1, 2, 3)

- **职责**：梳理底层逻辑、知识地图与颠覆性认知。
- **输入**：书名、作者、{逻辑种子}、{章节蒸馏摘要}、[Python检索工具通过Bash调用]
- **输出**：
  - **模块1：一句话逻辑** — 揭示"到底在解决什么根本问题"
  - **模块2：核心知识地图** — 梳理核心支柱模块及串联关系
  - **模块3：颠覆性认知** — 挖掘最核心的反常识洞见

- **prompt模板：**
```
【变动说明】你的产出不再直接面向 PPT，而是面向可生长的 Markdown 知识库。
你输出的每一项都将成为独立的 Wiki 页面，供 AI 问答和增量更新使用。

你是一位拥有15年经验的知识架构师，擅长从宏观视角拆解复杂知识体系。

书名：{书名}
作者：{作者}

【逻辑种子】
{逻辑种子全文}

【章节蒸馏摘要】（若有）
{章节蒸馏摘要}

【检索工具】（仅在>60万字时可用，通过Bash调用）
- 语义检索：python /tmp/book_rag/search.py "查询内容" [top_k]
- 图谱检索：python /tmp/book_rag/graph.py --entity "实体名" [--depth N]
- 多跳推理：python /tmp/book_rag/multihop.py "起始实体" "目标实体" [max_hops]

任务：产出以下三个模块（中文，markdown格式）。每个模块末尾标注【设计约束：适合的布局类型】。

## 模块1：一句话逻辑
- 用一句话精准概括全书的核心底层逻辑
- 禁止空洞赞美，必须揭示「这本书到底在解决什么根本问题」
- 末尾标注设计约束

## 模块2：核心知识地图
- 梳理全书的核心支柱模块，数量与结构准确反映原书的逻辑体系
- 【重要】不要硬性压缩。如果原书有7个递进原则，就呈现7个
- 用逻辑种子中的 Logic Thread 来组织模块间的串联关系
- 【若信息不足】通过Bash调用图谱检索：python /tmp/book_rag/graph.py --entity "核心概念" --depth 2
- 每个支柱格式：
  - 【名称】+ 【一句话本质】
  - **推导过程**（引用原文 >）
  - **逻辑串联**：使用 [[双链]] 指向其他支柱或概念，如 [[共同目的]]、[[安全感]]
  - **关联实体**：标注涉及的 [[实体名]]
- 末尾总结整体逻辑流并标注设计约束

## 模块3：颠覆性认知
- 挖掘书中最核心的反常识洞见，少而精
- 【若信息不足】通过Bash调用多跳推理：python /tmp/book_rag/multihop.py "旧认知" "新认知" 3
- 每个洞见格式：
  - **观点陈述**
  - **常见误解**
  - **书中真相**（引用原文 >）
  - **推导路径**：从 [[旧认知]] → [[关键转折]] → [[新认知]]
  - 与核心隐喻关联，说明如何支撑全书论点
- 末尾标注设计约束
```

---

#### Agent Beta: 实战派 (模块4)

- **职责**：将方法论转化为"准备-执行-复盘"闭环。
- **输入**：书名、作者、{逻辑种子}、{章节蒸馏摘要}、[Python检索工具通过Bash调用]
- **输出**：
  - **模块4：落地行动指南** — 准备→执行→复盘 + 场景模拟 + 自检卡

- **prompt模板：**
```
你是一位擅长将理论转化为实践的行动教练，专注于可执行的闭环设计。

书名：{书名}
作者：{作者}

【逻辑种子】
{逻辑种子全文}

【章节蒸馏摘要】（若有）
{章节蒸馏摘要}

【检索工具】（仅在>60万字时可用，通过Bash调用）
- 语义检索：python /tmp/book_rag/search.py "查询内容" [top_k]
- 图谱检索：python /tmp/book_rag/graph.py --entity "实体名" [--depth N]
- 多跳推理：python /tmp/book_rag/multihop.py "起始实体" "目标实体" [max_hops]

任务：产出以下模块（中文，markdown格式）。每个子模块末尾标注【设计约束：适合的布局类型】。

## 模块4：落地行动指南

### 4.1 准备-执行-复盘指南
- 将方法论转化为「准备→执行→复盘」三阶段
- 使用逻辑种子中的术语表，保持作者原生表达
- 【关键】若缺失具体操作步骤，通过Bash调用语义检索：
  python /tmp/book_rag/search.py "具体操作步骤" 10
- 每个阶段：阶段目标 → 自我审视问题 → 具体步骤（带关键话术）→ 原则提醒
- 每步骤末尾标注可能踩坑点，关联到 [[边界约束X]]
- 末尾标注设计约束

### 4.2 应用场景模拟（Markdown 对话体格式）
- 【场景A】职场场景：对话体，展示关键方法的完整循环
  - 格式：
    ```
    【对方】"..."
    【错误反应】"..."
    【内心】（错误反应的内心活动）
    ---
    【理想路径】"..."
    【内心】（理想路径的内心活动）
    ```
  - 必须包含：「对方 / 错误反应 / 理想路径」三段式对比
  - 完整循环（如综合陈述法的5步）
  - 关联踩坑点：[[边界约束X]]
- 【场景B】个人场景：决策树，展示关键决策的完整路径
  - 包含分支判断和回退逻辑
- 末尾标注设计约束

### 4.3 每日快速自检卡
- 表格形式：阶段 → 自我审视问题 → 触发行动
- 末尾标注设计约束
```

---

#### Agent Gamma: 策展人 (模块5, 6, 7, 8)

- **职责**：提炼金句、标注蒸馏遗珠、规划延伸路径、定义边界约束。
- **输入**：书名、作者、{逻辑种子}、{章节蒸馏摘要}、[Python检索工具通过Bash调用]
- **输出**：
  - **模块5：金句复刻** — 具有传播力的总结性话术
  - **模块6：知识蒸馏清单** — 透明化声明省略内容
  - **模块7：延伸行动地图** — 下一步行动与关联作品
  - **模块8：边界约束与避坑指南**（V4.0 新增）

- **prompt模板：**
```
你是一位擅长提炼金句的资深编辑兼学习路径设计师。

书名：{书名}
作者：{作者}

【逻辑种子】
{逻辑种子全文}

【章节蒸馏摘要】（若有）
{章节蒸馏摘要}

【检索工具】（仅在>60万字时可用，通过Bash调用）
- 语义检索：python /tmp/book_rag/search.py "查询内容" [top_k]
- 图谱检索：python /tmp/book_rag/graph.py --entity "实体名" [--depth N]

任务：产出以下四个模块（中文，markdown格式）。每个模块末尾标注【设计约束：适合的布局类型】。

## 模块5：金句复刻
- 提炼具有传播力的总结性话术，形式多样，功能性优先
- 【关键】通过Bash调用语义检索获取书中原话：
  python /tmp/book_rag/search.py "核心洞见关键词" 5
- 标注每句对应的洞见主题 + 形式类型（格言式/两段式/对话式/反问式/对比式）
- 末尾标注设计约束

## 模块6：知识蒸馏清单
- 列出「被省略但值得读者留意」的内容
- 【关键】通过Bash调用图谱检索检查是否有核心逻辑链被遗漏：
  python /tmp/book_rag/graph.py --entity "核心概念" --depth 3
- 每项：【内容名称】+ 省略理由 + 核心应对原则速查索引
- 末尾标注设计约束

## 模块7：延伸行动地图
- 3-5个「读完后的下一步行动」建议
- 每个：行动标题 + 具体做法 + 推荐理由 + 可搜索关键词（中英文各一）
- 主动关联同一作者/团队的相关作品序列
- 末尾标注设计约束

## 模块8：边界约束与避坑指南（V4.0 新增）
- 🚫 **红线清单**（❌ 绝对不能做X，因为会导致Y）
  - 3-5条绝对禁忌行为
  - 每项：禁忌行为 + 严重后果 + 替代方案
- ⚠️ **致命误区**（误区：以为做A就行 → 真相：必须先做B）
  - 3-5个常见错误认知
  - 每项：错误认知 + 正确认知 + 推导依据
- 🛡️ **适用边界**（仅在 [条件1] 且 [条件2] 时适用）
  - 明确说明本书方法论的适用范围和前提条件
  - 每项：适用条件 + 不适用场景 + 判断标准
- 使用 [[双链]] 关联到核心支柱和概念
- 末尾标注设计约束
```

**执行方式：**
在**单条消息**中同时发起3个Agent tool调用，全部前台执行。

---

### Phase 2: 一致性守护与叙事流缝合（V4.0 升级版）

1. **逻辑对齐**：
   - 检查 Agent Beta 的动作是否能解决 Agent Alpha 提出的根本问题
   - 检查 Agent Gamma 的金句/延伸是否与核心论点一致
   - 确保模块8 的边界约束与模块2 的核心支柱逻辑自洽

2. **术语校准**：强制使用 Phase 0 提取的 Unique Glossary，替换泛化表述。检查所有 Agent 产出中的术语一致性。

3. **图谱校验**（>60万字时）：通过Bash调用Python验证各模块间的逻辑连接是否在图谱中存在有效路径：
   ```bash
   python /tmp/book_rag/graph.py --entity "模块A核心概念" --depth 3
   ```

4. **跨 Wiki 一致性检查**（V4.0 新增）：
   - [[双链]] 指向核实：检查所有 `[[概念名]]` 是否在 INDEX.md 中有对应条目
   - 矛盾标记：如果不同模块间存在逻辑矛盾，用 `<!-- CONFLICT: ... -->` 标记
   - 术语统一：确保同一概念在所有模块中使用相同的 [[双链]] 名称
   - 孤立页面检测：标注 INDEX.md 中未被任何 [[双链]] 引用的条目

5. **图谱校验增强**（V4.0 新增）：
   - 验证关键概念间在图谱中存在有效路径
   - 若路径缺失，通过 Bash 调用多跳推理补全：
     ```bash
     python /tmp/book_rag/multihop.py "概念A" "概念B" 5
     ```
   - 将补全的路径作为新关系写入 INDEX.md 的关联实体部分

6. **注入导航线**：在每一页内参底部标注其在原书逻辑旅程中的位置（如：[认知重塑] 30%）

7. **质量校验清单**：
   - [ ] 模块1：精确到"不可替换"？揭示根本问题？
   - [ ] 模块2：支柱数量反映原书结构？逻辑关系清晰？图谱路径完整？
   - [ ] 模块3：真的"反常识"？有原文证据支撑？
   - [ ] 模块4：准备→执行→复盘完整？检索依据真实？场景展示完整循环？
   - [ ] 模块5：形式多样？原话复刻不失真？
   - [ ] 模块6：核心实战工具保留框架？逻辑链无遗漏？
   - [ ] 模块7：关联同作者作品？有搜索关键词？
   - [ ] 模块8：红线清晰？误区准确？边界明确？
   - [ ] 一致性：所有模块围绕同一核心隐喻？术语统一？[[双链]] 可解析？
   - [ ]（V4.0 新增）[[双链]] 无孤立页面？无空指向？

8. **内容润色**：统一语气，去除重复，确保模块间形成连贯叙事

---

### Phase 3: 双轨产出（V4.0 全新设计）

#### 风格决策树

```
用户提供了风格关键词？
├─ "杂志风" / "magazine" → 路径B：杂志风 HTML PPT（editorial-magazine 主题，10种布局模板填充）
├─ 其他风格关键词 → 路径B：传统风格 HTML PPT（CSS变量体系 + 组件库）
│       "麦肯锡" → 深蓝背景 + 蓝色强调 + 分析框架布局
│       "极简" → 白色背景 + 黑色文字 + 大量留白
│       "学术" → 浅灰背景 + 深色文字 + 引用框 + 脚注
│       "科技感" → 深色渐变 + 霓虹色 + 几何装饰
│       "暖色" → 米色背景 + 橙棕强调 + 圆角卡片
│       "安全对话" → 暖色系 + 圆角 + 柔和渐变 + 大量留白
│       "智能匹配" → 结合关键词和书籍情感基调
│
└─ 未提供 → 智能匹配书籍情感基调（基于Meta-Value判断）
        若无法判断 → 默认使用杂志风主题
```

#### 产出物

| 路径 | 产出文件 | 说明 |
|:---|:---|:---|
| 路径A | `{书名}_精华内参.md` | Markdown 知识库，保留所有 [[双链]] 语法 |
| 路径B | `{书名}_精华内参.html` | HTML PPT（杂志风或传统风格） |
| 知识库 | `{书名}-kb/wiki/INDEX.md` | Wiki 路由文件，可增量更新 |

---

#### 路径A：Markdown 知识库合并

1. 收集 Phase 1 三个 Agent 的完整 markdown 输出
2. 按模块顺序拼接：模块1 → 模块2 → 模块3 → 模块4 → 模块5 → 模块6 → 模块7 → 模块8
3. 保留所有 [[双链]] 语法，确保跨模块链接一致
4. 前置 YAML frontmatter（书名、作者、逻辑种子摘要）
5. 写入 `{书名}_精华内参.md`
6. 同步更新 `{书名}-kb/wiki/INDEX.md`，填充完整的条目链接

---

#### 路径B：杂志风 HTML PPT 生成

**10 种布局模板详解：**

| 布局类 | 视觉结构 | 最佳内容 | 典型 slide 类 |
|:---|:---|:---|:---|
| `.hero-cover` | 居中巨标题 + 副标题 + kicker + 底部元数据 | 封面、模块1一句话逻辑 | hero light/dark |
| `.act-divider` | kicker + 超大章节号 + 标题 + 引语 | 章节切换、模块间过渡 | hero dark |
| `.big-numbers` | 3x2 网格 stat-card（数字 + 标签 + 小字说明） | 关键数据、统计、核心指标 | light |
| `.text-image-split` | grid-2-7-5 左文右图（引用 + 说明 + 图片） | 概念解释、支柱展开 | light |
| `.image-grid` | 固定 height 的图片网格（2-4 张） | 案例对比、多图展示 | light |
| `.pipeline` | 垂直流程 + 圆点节点 + 连接线 | 步骤、阶段、方法论流程 | light |
| `.question-page` | 巨型问号 + 大问题 + 引语 | 悬念页、核心问题引出 | hero light |
| `.big-quote` | 大号衬线引用 + 作者 + 出处 | 金句、核心引用 | dark |
| `.before-after` | 左右并列对比（旧 vs 新、错误 vs 正确） | 认知对比、方法论前后效果 | light |
| `.mixed-layout` | 灵活图文混排（多列 + 引用 + 列表） | 信息密集页、综合内容 | light/dark |

**设计约束（硬性规则）：**
1. 每页 `<section class="slide">` 必须带 light / dark / hero 类
2. 连续 3 页以上相同主题 = 不允许；必须 light 和 dark 交替编排
3. 每页使用预定义布局类名（来自 editorial-magazine.css），禁止自定义布局
4. 字体类必须使用 ed-* 系列：
   - 标题/数字 → `.ed-display-zh` / `.ed-h1` / `.ed-h2` / `.ed-big-num`
   - 正文 → `.ed-body`
   - 元数据 → `.ed-kicker` / `.ed-meta`
5. 每页必须包含 `.chrome`（页眉：书名 + 章节标记）和 `.foot`（页脚：页码 + 导航信息）
6. 入场动效：在需要动效的元素上使用 `data-anim` 属性（可选值：fade-up / fade-right / fade-left / rise-in / zoom-pop / stagger-list）
7. 图片使用 `.ed-frame-img` + 比例类（`.ed-r-16x9` / `.ed-r-16x10` / `.ed-r-4x3` / `.ed-r-3x2` / `.ed-r-1x1`）
8. 禁止使用 emoji，用文字或符号代替
9. 禁止自定义渐变、阴影、圆角（继承主题定义）
10. 引用块使用 `.ed-callout` / `.ed-q-big` / `.ed-cite`

**5 套主题色切换（T 键）：**

| 主题 | data-theme | 适用场景 |
|:---|:---|:---|
| 墨水经典 | `ink` | 通用/商业/默认 |
| 靛蓝瓷 | `indigo` | 科技/研究/数据 |
| 森林墨 | `forest` | 自然/文化/非虚构 |
| 牛皮纸 | `leather` | 怀旧/人文/文学 |
| 沙丘 | `dune` | 艺术/设计/创意 |

**页面类型与主题组合规则：**
- `.slide.hero.light` — 浅色 hero，适合开场封面、问题页
- `.slide.hero.dark` — 深色 hero，适合幕封、大引用页
- `.slide.light` — 浅色正文页，适合数据、流程、图文混排
- `.slide.dark` — 深色正文页，适合引用对比、金句展示
- hero 页占比约 20-30%，non-hero 页 70-80%，形成阅读节奏

**Phase 3 Prompt（路径B）：**

```markdown
你是一位杂志排版设计师。基于前面 3-Agent 产出的知识库内容，生成一本杂志风 HTML PPT。

【内容输入】
{3-Agent 合并输出全文}

【设计约束】
1. 每页 <section class="slide"> 必须带 light / dark / hero 类
2. 连续 3 页以上相同主题 = 不允许
3. 每页使用预定义布局类名（来自 editorial-magazine.css）：
   - .hero-cover / .act-divider / .big-numbers / .text-image-split
   - .image-grid / .pipeline / .question-page / .big-quote / .before-after / .mixed-layout
4. 字体类必须使用 ed-* 系列：
   - 标题/数字 → .ed-display-zh / .ed-h1 / .ed-h2 / .ed-big-num
   - 正文 → .ed-body
   - 元数据 → .ed-kicker / .ed-meta
5. 每页必须包含 .chrome（页眉）和 .foot（页脚）
6. 入场动效：在需要动效的元素上使用 data-anim 属性
7. 图片使用 .ed-frame-img + 比例类（.ed-r-16x9 / .ed-r-16x10 等）
8. 禁止使用 emoji，用文字或符号代替
9. 禁止自定义渐变、阴影、圆角
10. 引用块使用 .ed-callout / .ed-q-big / .ed-cite

【布局模板选择指南】
- 封面/大标题页 → .hero-cover
- 章节切换页 → .act-divider
- 数据/统计/关键数字 → .big-numbers
- 概念解释+配图 → .text-image-split
- 多图展示/案例对比 → .image-grid
- 流程/步骤/阶段 → .pipeline
- 悬念/核心问题 → .question-page
- 金句/核心引用 → .big-quote
- 前后对比/新旧对比 → .before-after
- 信息密集/综合页面 → .mixed-layout

【输出格式】一个完整的 HTML 文件，内联引用 editorial-magazine.css 主题。
```

---

#### 路径B（传统风格）：CSS变量体系 + 组件库

当用户指定非杂志风风格时，使用原有 V3.2 的 CSS 变量体系：

默认风格：
```css
:root {
  --bg: #0e1117;
  --surface: #161b22;
  --accent: #e63946;
  --accent-dim: #c5303c;
  --heading: #f0f6fc;
  --text: #b1bac4;
  --text-dim: #6e7681;
  --border: #21262d;
  --tag-bg: rgba(230,57,70,0.12);
}
```

**组件库：**

| 类名 | 视觉表现 | 适用场景 |
|:---|:---|:---|
| `.hero-box` | 渐变背景 + 巨型文字 + 核心隐喻标签 | 模块1（一句话逻辑） |
| `.logic-map` | 连线布局 + 章节节点 + 递进箭头 | 模块2（知识地图） |
| `.graph-view` | 节点连线 + 力导向布局 | 模块2/3（逻辑关系图谱展示） |
| `.contrast-card` | 红绿/深浅对比色块 | 模块3/4（认知对比/错误vs理想） |
| `.step-flow` | 垂直进度条 + 阶段标签 + 自审视问题 | 模块4（行动指南） |
| `.dialogue-box` | 角色标签 + 对白 + 内心独白 | 模块4（场景模拟对话体） |
| `.decision-tree` | 分支节点 + 判断条件 + 路径线 | 模块4（场景模拟决策树） |
| `.cycle-diagram` | 环形节点 + 箭头循环 | 模块4（方法论完整循环） |
| `.quote-glass` | 毛玻璃质感 + 引号装饰 | 模块5（金句） |
| `.check-table` | 阶段-问题-行动三列表格 | 模块4（自检卡） |
| `.nav-bar` | 底部导航线 + 百分比进度 | 全局（逻辑旅程位置） |

**HTML结构规范：**
1. 每个模块占1-N个 `.slide`，`min-height:100vh` 全屏分页
2. `scroll-snap-type: y mandatory` 分页滚动
3. 纯HTML+内联CSS，零外部依赖
4. 响应式：`@media (max-width: 768px)` 移动适配
5. 每页底部 `.nav-bar` 显示逻辑旅程位置

**排版规则：**
- `h1`: 衬线体900, 3rem
- `h2`: 衬线体700, 1.9rem, 左侧强调色边框
- `h3`: 无衬线体600, 1.05rem, 强调色大写
- 正文: 无衬线体400, 0.98rem, 行距1.75
- 关键词: 加粗+强调色

---

### Phase 4: 交付与质量门控（V4.0 增强版）

#### 交付物写入

1. **路径A 交付**：`{原文件名}_精华内参.md`
   - 写入 `{书名}-kb/output/` 目录
   - 同步更新 `wiki/INDEX.md` 最终版本（所有 [[双链]] 指向已核实）
   - 若路径B 也产出，在 Markdown 末尾添加 HTML 版查看指引

2. **路径B 交付**：`{原文件名}_精华内参.html`
   - 写入 `{书名}-kb/output/` 目录
   - 确保零外部依赖，浏览器直接打开
   - 引用 editorial-magazine.css（若杂志风）或内联 CSS（若传统风格）

3. **Wiki 知识库同步**：
   - 根据 Phase 1 各 Agent 的实际产出，创建对应的概念/行动/边界/实体 markdown 文件
   - 每个文件遵循标准模板：标题 + 一句话摘要 + 正文 + 关联 [[双链]] + 原文引用
   - 更新 INDEX.md 中的条目链接为实际路径

#### 交付摘要输出

1. 文件路径 + 文件大小（路径A 和 路径B 分别列出）
2. 总页数 + 各模块页数分布
3. 核心隐喻（供用户确认蒸馏是否命中）
4. 使用的处理路径（标准/分层压缩/三级蒸馏/Python认知图谱）
5. **图谱摘要**（>60万字时）：实体数量、关系数量、最核心的逻辑链
6. **检索日志**（>60万字时）：各Agent调用了几次检索工具？最核心的查询是什么？
7. **Wiki 统计**（V4.0 新增）：概念文件数、行动文件数、边界文件数、实体文件数、[[双链]] 总连接数

#### 质量检查清单

**杂志风路径（P0 级，必须通过）：**
- [P0] 字体分工正确：标题用 ed-display-zh/ed-h1 等衬线类，正文用 ed-body
- [P0] 主题交替合理：没有连续 3 页相同 light/dark，hero 占比 20-30%
- [P0] 没有使用 emoji（用文字或符号代替）
- [P0] 图片使用标准比例类（ed-r-16x9 / ed-r-16x10 / ed-r-4x3 / ed-r-3x2 / ed-r-1x1）
- [P0] 没有自定义渐变色、阴影、圆角（所有样式继承自主题）
- [P0] 布局类全部来自预定义的 10 种模板，无自定义布局

**杂志风路径（P1 级，推荐通过）：**
- [P1] 入场动效使用了 data-anim 属性
- [P1] 每页都有 chrome（页眉）+ foot（页脚）
- [P1] 引用块使用了 ed-callout / ed-q-big / ed-cite
- [P1] 所有页面带有 data-title 属性（用于目录和概览）

**传统风格路径：**
- [ ] 使用预定义组件类名（无自定义 CSS）
- [ ] 每页底部有 .nav-bar 逻辑旅程位置
- [ ] 响应式适配 @media (max-width: 768px)

**自检命令：**
```bash
# 检查是否包含 emoji
grep -n '[^\x00-\x7F]' {书名}_精华内参.html | head -20
# 检查主题交替（统计 light/dark/hero 出现次数）
grep -o 'class="slide [^"]*"' {书名}_精华内参.html | sort | uniq -c
# 检查是否使用了标准比例类
grep -o 'ed-r-[a-z0-9-]*' {书名}_精华内参.html | sort -u
# 检查是否包含自定义圆角/阴影/渐变
grep -n 'border-radius\|box-shadow\|linear-gradient\|radial-gradient' {书名}_精华内参.html
# 检查布局类使用情况
grep -oP 'class="[^"]*slide[^"]*"' {书名}_精华内参.html | head -30
```

#### 最终提醒

提示用户：
- 浏览器直接打开 HTML 文件即可查看
- 按 S 键开启演讲者模式
- 按 T 键切换主题色（杂志风路径）
- Markdown 知识库可供 AI 问答和增量更新

---

## 降级策略

当Python依赖不可用或图谱构建失败时，自动降级：

```
Python可用？
├─ 是 → 完整GraphRAG路径（FAISS + NetworkX + Agent检索）
│       Phase 0: 完整逻辑种子 + 图谱索引 + Wiki骨架
│       Phase 1: 3-Agent 通过Bash调用Python检索
│       Phase 2: 图谱路径校验
│       Phase 3: 双轨产出（Markdown + HTML）
│
└─ 否 → 降级路径：主控Agent深度阅读
         ├─ 主控Agent用Read逐章精读，手动提取关键段落
         ├─ 手动构建逻辑图谱摘要（markdown格式）
         │   ├─ 核心概念列表 + 关系描述
         │   ├─ 跨章节逻辑流
         │   └─ 作者核心术语表
         ├─ 将精炼后的信息分发给子Agent
         └─ 子Agent无需Python工具，直接处理摘要
```

**降级路径下的 V4.0 特性支持：**
- Phase 0 Wiki 骨架创建：仍然可用（基于逻辑种子而非图谱）
- Phase 1 [[双链]] 体系：仍然可用（基于手动构建的概念关系）
- Phase 3 双轨产出：仍然可用（路径A Markdown 知识库 + 路径B 杂志风 HTML）
- Phase 4 质量检查：完全可用
- 唯一损失：图谱驱动的自动多跳推理和跨章节关系发现

**降级触发条件：**
- `pip install` 失败（网络受限或无权限）
- Python 运行环境缺失
- 文本过长导致内存不足（>100万字极端情况）
- FAISS/NetworkX 构建超时

---

## 质量标准

1. **一句话逻辑**：精确到"不可替换"，揭示根本问题
2. **知识地图**：支柱数量反映原书结构，不硬性压缩，逻辑链完整
3. **颠覆性认知**：少而精，有原文证据支撑，与核心隐喻关联
4. **行动指南**：准备→执行→复盘完整，检索依据真实，场景展示完整循环，有错误vs理想对比
5. **金句**：形式多样，功能性优先，原话复刻不失真
6. **蒸馏清单**：核心实战工具保留框架，逻辑链无遗漏，省略理由透明
7. **行动地图**：关联同作者作品，有搜索关键词
8. **边界约束**（V4.0）：红线清单清晰，致命误区准确，适用边界明确
9. **一致性**：所有模块围绕同一核心隐喻，术语统一，[[双链]] 可解析
10. **导航线**：每页标注逻辑旅程位置
11. **HTML**：零依赖，浏览器直接打开，5秒/页可扫描（V4.0 杂志风增加字体分工正确、主题交替合理、无 emoji 检查）
